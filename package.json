{
  "name": "staynest-hotel",
  "version": "1.0.0",
  "description": "Hotel website with an admin content manager",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-basic-auth": "^1.2.1",
    "multer": "^2.2.0"
  }
}
